import React from 'react';
import { Link } from 'react-router-dom';

function EmailSearchResults(props) {
    return (
        <div className="emailSearchResults">These are search results.</div>
    );
};

export default EmailSearchResults;